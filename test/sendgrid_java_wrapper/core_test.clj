(ns sendgrid-java-wrapper.core-test
  (:use midje.sweet)
  (:require [sendgrid-java-wrapper.core :refer :all]))

(facts "abouts sendrid java wrapper"
       (fact "it needs tests"
             0 => 1))

